verbesserungen:

	eingabe der größen der konfiguration Über den file

	fuktion für die eingabe erstellen statt doppelt zu schreiben --> cleaner

	wenn in der file eingabe 0 oder nichts (wahrscheilich einfacher mit 0) steht soll dieser platz Übersprungen werden
	der witz ist das die top variable bei 0 bleibt
	
extra:

	ich habe die konfig classe so verändert das alles bei 1 anfängt so das der 0 index "leer" oder "nicht gefunden" heißt

	 